spring:
  docker:
    compose:
      host: 172.18.0.1
      file:
        - docker/docker-compose.yml
        - docker/docker-compose-ci.yml

  datasource:
    url: "jdbc:postgresql://jme-deploymentlog-db-local:5432/jme-deploymentlog-db-local"
